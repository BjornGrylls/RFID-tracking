@model RFID_tracking_web_MVC.Models.PanelHomeViewModel;

@{
    ViewData["Title"] = "Home";
}

<div class="container; w-100">
    <div class="row">
        <div class="col-sm" style='background: #03FCE8'>
            <h1 class="d-flex justify-content-center">På vej ind i boksen</h1>
            <p>
                @Html.ActionLink("Liste", "List", new { inSafe=false })
            </p>

            <table class="table">
                @*                <thead>
                <tr>
                <th>
                @Html.DisplayNameFor(model => model.tags.First().EPC96)
                </th>
                <th>
                @Html.DisplayNameFor(model => model.tags.First().PeakRSSI)
                </th>
                <th>Status</th>
                <th>Link</th>
                </tr>
                </thead>*@
                <tbody>
                    @foreach (var item in Model.list) {
                        @if (item.WeaponStatus != WeaponStatus._1) {
                            <tr>
                                @*                                <td>
                        @Html.DisplayFor(modelItem => item.EPC96)
                        </td>
                        <td>
                        @Html.DisplayFor(modelItem => item.PeakRSSI)
                        </td>
                        <td>
                        @Html.DisplayFor(modelItem => weapon.Status)
                        </td>*@
                                <td class="align-middle">
                                    @Html.DisplayFor(modelItem => item.ShooterName)
                                </td>
                                <td class="d-flex align-middle" style="font-size:5vw">
                                    @{
                                        if (item.WeaponName != "Ukendt!") {
                                            @Html.ActionLink(item.WeaponName, "ConfirmationIn", new { weaponId=item.WeaponId })
                                        } else {
                                            <p>Ukendt!</p>
                                        }
                                    }
                                </td>
                                

                            </tr>
                        }
                    }
                </tbody>
            </table>


        </div>
        <div class="col-sm" style='background: #33C827'>
            <h1 class="d-flex justify-content-center">På vej ud af boksen</h1>
            <p class="d-flex justify-content-end">
                @Html.ActionLink("Liste", "List", new { inSafe=true })
            </p>

            <table class="table">
@*                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.tags.First().EPC96)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.tags.First().PeakRSSI)
                        </th>
                        <th>Status</th>
                        <th>Link</th>
                    </tr>
                </thead>
*@                <tbody>
                    @foreach (var item in Model.list) {
                        @if (item.WeaponStatus == WeaponStatus._1) {
                            <tr>
@*                                <td>
                                    @Html.DisplayFor(modelItem => item.EPC96)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.PeakRSSI)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => weapon.Status)
                                </td>
                        *@
                                <td class="d-flex justify-content-center" style="font-size:5vw">
                                    @{
                                        if (item.WeaponName != "Ukendt!") {
                                            @Html.ActionLink(item.WeaponName, "SpecifyUser", new { weaponId=item.WeaponId })
                                        } else {
                                            <p>Ukendt!</p>
                                        }
                                    }
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>


        </div>
    </div>
</div>





